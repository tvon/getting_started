%ul#posts
  - posts.each do |post|

    %li.post

      %h2= post.title

      .byline
        <strong>By:</strong> #{post.author}

      .body
        = post.body

      - if user_signed_in?
        .actions
          = link_to('Destroy', post, :method=>:delete) 
