$('#comments').append("<%= escape_javascript(render comment) %>");
$('#comment_body').val('');
$('.comment').last().addClass('new');
$('html,body').animate({scrollTop: $('.comment').last().offset().top});
